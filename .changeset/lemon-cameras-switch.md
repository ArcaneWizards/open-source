---
'@arcanewizards/midi': patch
---

Add logging interface to midi module

Make it possible to provide a log interface (e.g. console) to the `midi`
initializer function, allowing for the module to log messages.
