---
'@arcanewizards/clx': patch
---

Make Active optional for compatibility with Clockworks Server

The `Active` field of control packets is not always present,
for example when using Server with Serato.
